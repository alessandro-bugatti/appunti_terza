#include <iostream>

using namespace std;

int main()
{
    const int DIM = 1000;
    int v[DIM];
    int N, M;
    cin >> N >> M;
    //Input di tutti gli elementi del vettore
    for (int i = 0; i < N; i++)
        cin >> v[i];
    for (int i = 0; i < M; i++)
    {
        int indice;
        cin >> indice;
        //Eseguo l'algoritmo, da completare

    }
    return 0;
}
